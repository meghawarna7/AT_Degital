import React from 'react';

import 'bootstrap/dist/css/bootstrap.min.css'; 


const Button1 = ({ text, onClick }) => {
  return (
    <div>
    <button className="btn btn-warning">Get free consultation</button></div>
  );
};

export default Button1;
